//Has been implemented in handleNewToDo.ts, no longer needed as a module.
/* export function validate(): void {
    let toDoMessage: string = document.getElementById("to-do-msg").value; //Ignore warning for now.
    if(toDoMessage.length === 0) {
        alert("Your todo message is too short, please try something longer.")
    }
} */